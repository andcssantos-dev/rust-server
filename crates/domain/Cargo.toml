[package]
name = "aurenfall-domain"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
aurenfall-core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }

[lints]
workspace = true
